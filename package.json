{
  "name": "zod-getting-started",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "postinstall": "npm run generate:data",
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "generate:data": "node api/generator.js",
    "server": "json-server api/db.json --port 3001"
  },
  "devDependencies": {
    "@faker-js/faker": "^7.3.0",
    "json-server": "^0.17.0",
    "typescript": "^4.6.4",
    "vite": "^3.0.0"
  },
  "dependencies": {
    "zod": "^3.17.10"
  }
}